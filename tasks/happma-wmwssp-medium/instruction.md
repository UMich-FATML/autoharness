Implement the fixed-allocation sample-size formula in `/app/wmwssp/R/samplesize.R` so the balanced designs reproduce the paper's main results for both the epilepsy and nasal-mucosa examples. The paper reports a balanced `24/24` design with total sample size `48` for Table 2 and a balanced `85/85` design with total sample size `170` for Table 4.

The codebase already contains the utility functions, the paper datasets, and the rest of the package surface. The current `WMWssp` implementation is a simplified placeholder that uses the wrong variance approximation. You can modify the R source, run `Rscript /app/wmwssp/eval/run_medium.R`, observe the metrics in `/app/results.json`, and iterate.

Write your final result to `/app/results.json` as a JSON object with exactly these keys: `epilepsy_balanced_raw_n`, `epilepsy_balanced_total_n`, `epilepsy_balanced_n1`, `epilepsy_balanced_n2`, `nasal_balanced_raw_n`, `nasal_balanced_total_n`, `nasal_balanced_n1`, and `nasal_balanced_n2`. Your score is based on how close the measured sample sizes are to the paper's reported balanced-design results, so partial implementations that move the values toward the targets receive partial credit.
