Implement the paper's allocation-optimization and Noether comparison helpers in `/app/wmwssp/R/samplesize.R`. The target rows are the unbalanced epilepsy result from Table 2 (`23/24`, total `47`), the epilepsy Noether comparison from Table 2 (`26/26`, total `52`), and the unbalanced nasal-mucosa result from Table 4 (`83/87`, total `170`).

The fixed-allocation `WMWssp` function is already present, but the optimization and comparator helpers are simplified placeholders. You can modify the R source, run `Rscript /app/wmwssp/eval/run_hard.R`, observe the metrics in `/app/results.json`, and iterate.

Write your final result to `/app/results.json` as a JSON object with exactly these keys: `epilepsy_unbalanced_raw_n`, `epilepsy_unbalanced_total_n`, `epilepsy_unbalanced_n1`, `epilepsy_unbalanced_n2`, `epilepsy_noether_raw_n`, `epilepsy_noether_total_n`, `epilepsy_noether_n1`, `epilepsy_noether_n2`, `nasal_unbalanced_raw_n`, `nasal_unbalanced_total_n`, `nasal_unbalanced_n1`, and `nasal_unbalanced_n2`. Your score is based on how close the measured allocations are to the paper's reported optimized and Noether-comparison rows, so partial implementations that move the values toward the targets receive partial credit.
