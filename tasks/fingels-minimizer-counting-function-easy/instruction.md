Recreate the exact Example 18 `pi_k(w)` sequences for the two worked `m=6` minimizers discussed in `/app/paper/paper.md` by repairing the staged counting implementation in `/app/repo`.

Write `/app/output/example18_counts.json` with exactly these top-level keys: `k_values` and `counts`. `k_values` must be the integers `6` through `16` inclusive. `counts` must map `ACACAA` and `ACACAC` to lists of exact integer `pi_k(w)` values aligned with `k_values`.
